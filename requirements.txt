# Pinned to the exact versions confirmed passing the full test suite
# (65/65) on the development machine, Python 3.13.
pandas==3.0.5
python-dotenv==1.2.3
groq==1.7.0
google-genai==2.23.0
fastapi==0.141.1
uvicorn[standard]==0.53.0
httpx==0.28.1
